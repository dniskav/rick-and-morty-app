import { InifiniteScrollDirective } from './inifinite-scroll.directive';

describe('InifiniteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new InifiniteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
